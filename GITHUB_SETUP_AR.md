# 🚀 دليل ربط المشروع بـ GitHub

هذا الدليل سيساعدك في ربط مشروع مولد القصص التفاعلية بـ GitHub.

## 📋 المتطلبات

- Git مثبت على جهازك
- حساب GitHub
- ملفات المشروع جاهزة

## 🔧 خطوات الإعداد

### الطريقة السريعة (Windows)

```bash
setup_github.bat
```

هذا السكريبت سيقوم بكل شيء تلقائياً!

### الطريقة اليدوية

#### الخطوة 1: تهيئة مستودع Git

افتح Terminal/Command Prompt في مجلد المشروع:

```bash
cd storytelling_ai
git init
```

#### الخطوة 2: إضافة الملفات

```bash
git add .
```

#### الخطوة 3: إنشاء Commit أولي

```bash
git commit -m "Initial commit: Interactive Story Generator with AI"
```

#### الخطوة 4: إنشاء مستودع على GitHub

1. اذهب إلى [GitHub](https://github.com) وسجل الدخول
2. اضغط على أيقونة **"+"** في الزاوية العلوية اليمنى
3. اختر **"New repository"**
4. املأ تفاصيل المستودع:
   - **اسم المستودع**: `interactive-story-generator` (أو أي اسم تفضله)
   - **الوصف**: "Interactive Story Generator using Google Gemini AI"
   - **الظهور**: اختر Public أو Private
   - **لا تقم** بتهيئة README أو .gitignore (لدينا هذه الملفات بالفعل)
5. اضغط **"Create repository"**

#### الخطوة 5: ربط المستودع المحلي بـ GitHub

بعد إنشاء المستودع، GitHub سيعرض عليك الأوامر. استخدم هذه:

```bash
# إضافة المستودع البعيد (استبدل YOUR_USERNAME و REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# تغيير اسم الفرع إلى main (إن لزم الأمر)
git branch -M main

# رفع الملفات إلى GitHub
git push -u origin main
```

#### الخطوة 6: التحقق

اذهب إلى صفحة مستودعك على GitHub وتأكد من رفع جميع الملفات.

## 📝 أوامر Git الشائعة

### العمل اليومي

```bash
# فحص الحالة
git status

# إضافة التغييرات
git add .

# حفظ التغييرات
git commit -m "وصف التغييرات"

# رفع إلى GitHub
git push
```

### تحديث من GitHub

```bash
# جلب آخر التغييرات
git pull
```

### إنشاء فرع جديد

```bash
# إنشاء والانتقال لفرع جديد
git checkout -b feature/mيزة-جديدة

# رفع الفرع الجديد
git push -u origin feature/mيزة-جديدة
```

## 🔒 ملاحظات مهمة

### الملفات التي لن تُرفع إلى GitHub

الملفات التالية مستثناة (عبر `.gitignore`):
- `.env` - يحتوي على مفاتيح API (لا ترفعه أبداً!)
- `__pycache__/` - ملفات Python المؤقتة
- `venv/` - البيئة الافتراضية
- `outputs/*` - صور القصص المولدة
- `.streamlit/` - إعدادات Streamlit

### أفضل ممارسات الأمان

1. **لا ترفع ملف `.env` أبداً** - يحتوي على مفاتيح API الحساسة
2. **استخدم `env_template.txt`** - آمن للرفع لأنه لا يحتوي على مفاتيح حقيقية
3. **راجع التغييرات قبل الحفظ** - استخدم `git status` و `git diff`

## 🎯 هيكل المستودع على GitHub

مستودعك يجب أن يحتوي على:
```
interactive-story-generator/
├── frontend/
│   └── app.py
├── services/
│   ├── __init__.py
│   ├── gemini_service.py
│   └── image_service.py
├── outputs/
│   └── .gitkeep
├── .gitignore
├── env_template.txt
├── requirements.txt
├── setup.bat
├── run.bat
├── README.md
├── QUICK_START.md
├── TROUBLESHOOTING.md
└── GITHUB_SETUP.md
```

## 🔄 تحديث المستودع

عند إجراء تغييرات:

```bash
# 1. فحص ما تغير
git status

# 2. إضافة التغييرات
git add .

# 3. حفظ مع وصف واضح
git commit -m "إضافة ميزة: [الوصف]"

# 4. رفع إلى GitHub
git push
```

## ❓ حل المشاكل

### "Remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

### "Permission denied"
- تحقق من بيانات اعتماد GitHub
- استخدم SSH keys أو Personal Access Token

### "Large file warning"
- تأكد من أن الملفات الكبيرة في `.gitignore`
- استخدم Git LFS للملفات الكبيرة إن لزم الأمر

---

**مشروعك الآن على GitHub! 🎉**

