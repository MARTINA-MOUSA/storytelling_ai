# 🔐 دليل إضافة API Keys في Streamlit Cloud

## المشكلة
إذا ظهرت رسالة: `GEMINI_API_KEY not found`، فهذا يعني أن المفاتيح لم تُضف بشكل صحيح.

## الحل - خطوات مفصلة:

### 1. اذهب إلى Settings
- في صفحة التطبيق على Streamlit Cloud
- اضغط على أيقونة **⋮** (ثلاث نقاط) في الزاوية اليمنى العلوية
- اختر **"Settings"**

### 2. افتح Secrets
- في القائمة الجانبية، اضغط **"Secrets"**
- أو اذهب مباشرة إلى: `https://share.streamlit.io/your-app-name/settings`

### 3. أضف المفاتيح
في مربع النص، أضف المفاتيح بهذا الشكل **بالضبط**:

```toml
GEMINI_API_KEY = "AIzaSyAV8M86_wj9EUfMs407tvYmKIAwiAF7sKs"
CUSTOM_IMAGE_API_KEY = "efba5c5ee632cb0e5b13ad97948a15b4f805bd948f855e7553a8693faa798b2e59fb76df19a360c015133a36926a9d7c"
USE_CLIPDROP = "true"
```

### 4. احفظ
- اضغط **"Save"** في الأسفل
- التطبيق سيعيد التشغيل تلقائياً

### 5. تحقق
- انتظر حتى ينتهي إعادة التشغيل (30-60 ثانية)
- أعد تحميل الصفحة
- يجب أن يعمل التطبيق الآن!

---

## ⚠️ أخطاء شائعة:

### ❌ خطأ 1: استخدام `=` بدلاً من `=`
```toml
# خطأ ❌
GEMINI_API_KEY="your_key"

# صحيح ✅
GEMINI_API_KEY = "your_key"
```

### ❌ خطأ 2: عدم استخدام علامات الاقتباس
```toml
# خطأ ❌
GEMINI_API_KEY = your_key

# صحيح ✅
GEMINI_API_KEY = "your_key"
```

### ❌ خطأ 3: مسافات إضافية
```toml
# خطأ ❌
GEMINI_API_KEY = " your_key "

# صحيح ✅
GEMINI_API_KEY = "your_key"
```

### ❌ خطأ 4: استخدام `.env` format
```toml
# خطأ ❌ (هذا للـ .env file محلي)
GEMINI_API_KEY=your_key

# صحيح ✅ (هذا لـ Streamlit Secrets)
GEMINI_API_KEY = "your_key"
```

---

## 🔍 كيفية التحقق من أن المفاتيح موجودة:

### طريقة 1: من الكود
أضف هذا في بداية `streamlit_app.py` مؤقتاً للتحقق:

```python
import os
st.write("GEMINI_API_KEY exists:", bool(os.getenv("GEMINI_API_KEY")))
st.write("CUSTOM_IMAGE_API_KEY exists:", bool(os.getenv("CUSTOM_IMAGE_API_KEY")))
```

### طريقة 2: من Logs
- اذهب إلى Settings → Logs
- ابحث عن أي أخطاء متعلقة بـ API keys

---

## 📝 مثال كامل للـ Secrets:

```toml
# Gemini API Key (مطلوب)
GEMINI_API_KEY = "AIzaSyAV8M86_wj9EUfMs407tvYmKIAwiAF7sKs"

# Image API Key (اختياري - لتحسين جودة الصور)
CUSTOM_IMAGE_API_KEY = "efba5c5ee632cb0e5b13ad97948a15b4f805bd948f855e7553a8693faa798b2e59fb76df19a360c015133a36926a9d7c"

# Use Clipdrop API (true/false)
USE_CLIPDROP = "true"
```

---

## 🆘 إذا لم يعمل بعد:

1. **تأكد من الصيغة:**
   - يجب أن تكون بصيغة TOML
   - استخدام `=` مع مسافات
   - استخدام علامات اقتباس `"..."`

2. **أعد تشغيل التطبيق:**
   - Settings → "Reboot app"

3. **تحقق من المفاتيح:**
   - تأكد أن المفاتيح صحيحة
   - Gemini API Key من: https://makersuite.google.com/app/apikey

4. **تحقق من Logs:**
   - Settings → Logs
   - ابحث عن أخطاء

---

**بعد إضافة المفاتيح بشكل صحيح، التطبيق سيعمل! 🎉**

