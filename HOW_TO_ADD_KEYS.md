# 🔑 كيفية إضافة API Keys

## الطريقة 1: ملف .env (موصى به للمحلي) ✅

### الخطوات:

1. **أنشئ ملف `.env` في مجلد المشروع:**
   ```bash
   # Windows
   copy .env.example .env
   
   # Linux/Mac
   cp .env.example .env
   ```

2. **افتح ملف `.env` وأضف مفاتيحك:**
   ```env
   GEMINI_API_KEY=AIzaSyAV8M86_wj9EUfMs407tvYmKIAwiAF7sKs
   CUSTOM_IMAGE_API_KEY=efba5c5ee632cb0e5b13ad97948a15b4f805bd948f855e7553a8693faa798b2e59fb76df19a360c015133a36926a9d7c
   USE_CLIPDROP=true
   ```

3. **جاهز!** التطبيق سيقرأ المفاتيح تلقائياً

**✅ المميزات:**
- آمن (الملف في .gitignore)
- سهل
- يعمل محلياً

---

## الطريقة 2: ملف config.py (للاختبار فقط) ⚠️

### الخطوات:

1. **انسخ الملف:**
   ```bash
   copy config.py.example config.py
   ```

2. **افتح `config.py` وأضف مفاتيحك:**
   ```python
   GEMINI_API_KEY = "AIzaSyAV8M86_wj9EUfMs407tvYmKIAwiAF7sKs"
   CUSTOM_IMAGE_API_KEY = "efba5c5ee632cb0e5b13ad97948a15b4f805bd948f855e7553a8693faa798b2e59fb76df19a360c015133a36926a9d7c"
   USE_CLIPDROP = True
   ```

3. **جاهز!** التطبيق سيقرأ المفاتيح تلقائياً

**⚠️ تحذير:**
- هذا الملف في .gitignore
- **لا ترفعه إلى GitHub أبداً!**
- للاختبار المحلي فقط

---

## الطريقة 3: Streamlit Secrets (للنشر على الإنترنت) 🌐

### للاستخدام على Streamlit Cloud:

1. اذهب إلى **Settings → Secrets**
2. أضف:
   ```toml
   GEMINI_API_KEY = "AIzaSyAV8M86_wj9EUfMs407tvYmKIAwiAF7sKs"
   CUSTOM_IMAGE_API_KEY = "efba5c5ee632cb0e5b13ad97948a15b4f805bd948f855e7553a8693faa798b2e59fb76df19a360c015133a36926a9d7c"
   USE_CLIPDROP = "true"
   ```
3. اضغط "Save"

**✅ المميزات:**
- آمن تماماً
- للنشر على الإنترنت
- لا يظهر في الكود

---

## ترتيب الأولوية (أي طريقة تعمل أولاً):

1. **Environment Variables** (من .env أو النظام)
2. **Streamlit Secrets** (للنشر)
3. **config.py** (للاختبار المحلي)

---

## ⚠️ تحذيرات أمنية مهمة:

### ❌ لا تفعل:
- ❌ لا تضع المفاتيح مباشرة في الكود
- ❌ لا ترفع `.env` إلى GitHub
- ❌ لا ترفع `config.py` إلى GitHub
- ❌ لا تشارك المفاتيح مع أحد

### ✅ افعل:
- ✅ استخدم `.env` للمحلي
- ✅ استخدم Streamlit Secrets للنشر
- ✅ تأكد أن `.env` و `config.py` في `.gitignore`
- ✅ استخدم `config.py.example` كقالب فقط

---

## التحقق من أن المفاتيح تعمل:

بعد إضافة المفاتيح، شغّل التطبيق:
```bash
streamlit run frontend/app.py
```

إذا ظهرت رسالة خطأ، تحقق من:
1. الملف موجود في المكان الصحيح
2. الصيغة صحيحة
3. المفاتيح صحيحة

---

**بعد إضافة المفاتيح، التطبيق سيعمل! 🎉**

