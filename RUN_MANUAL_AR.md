# 🚀 دليل التشغيل اليدوي للتطبيق

## الخطوات المطلوبة لتشغيل التطبيق يدوياً

### 1️⃣ تفعيل البيئة الافتراضية (Virtual Environment)

**في Windows (PowerShell):**
```powershell
.\venv\Scripts\Activate.ps1
```

**أو في Command Prompt:**
```cmd
venv\Scripts\activate.bat
```

**ملاحظة:** إذا لم تكن البيئة الافتراضية مفعلة، ستحتاج إلى تثبيت المكتبات مباشرة.

### 2️⃣ التأكد من تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### 3️⃣ إعداد ملف البيئة (.env)

تأكد من وجود ملف `.env` في المجلد الرئيسي للمشروع مع المفتاح التالي:

```env
GEMINI_API_KEY=your_gemini_api_key_here
HUGGINGFACE_API_KEY=your_huggingface_key_here
```

**ملاحظة:** `HUGGINGFACE_API_KEY` اختياري، لكن `GEMINI_API_KEY` مطلوب.

### 4️⃣ تشغيل التطبيق يدوياً

**الطريقة الأساسية:**
```bash
streamlit run frontend/app.py
```

**أو مع تحديد المنفذ:**
```bash
streamlit run frontend/app.py --server.port 8501
```

**أو مع عنوان IP محدد:**
```bash
streamlit run frontend/app.py --server.address localhost --server.port 8501
```

### 5️⃣ فتح التطبيق في المتصفح

بعد تشغيل الأمر، سيتم فتح التطبيق تلقائياً في المتصفح على العنوان:
```
http://localhost:8501
```

إذا لم يفتح تلقائياً، افتح المتصفح يدوياً واذهب إلى العنوان أعلاه.

---

## 📋 الأوامر الكاملة (سطر واحد)

**في PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1; streamlit run frontend/app.py
```

**في Command Prompt:**
```cmd
venv\Scripts\activate.bat && streamlit run frontend/app.py
```

**بدون بيئة افتراضية (إذا كانت المكتبات مثبتة عالمياً):**
```bash
streamlit run frontend/app.py
```

---

## 🔧 خيارات إضافية للتشغيل

### تشغيل على منفذ مختلف:
```bash
streamlit run frontend/app.py --server.port 8502
```

### تشغيل مع إعدادات مخصصة:
```bash
streamlit run frontend/app.py --server.headless true
```

### تشغيل مع إعادة تحميل تلقائي معطل:
```bash
streamlit run frontend/app.py --server.runOnSave false
```

---

## ⚠️ حل المشاكل الشائعة

### المشكلة: "streamlit: command not found"
**الحل:**
```bash
pip install streamlit
```

### المشكلة: "GEMINI_API_KEY not found"
**الحل:**
- تأكد من وجود ملف `.env` في المجلد الرئيسي
- تأكد من كتابة المفتاح بشكل صحيح بدون مسافات

### المشكلة: "ModuleNotFoundError"
**الحل:**
```bash
pip install -r requirements.txt
```

### المشكلة: المنفذ 8501 مستخدم
**الحل:** استخدم منفذ آخر:
```bash
streamlit run frontend/app.py --server.port 8502
```

---

## 📝 ملاحظات مهمة

1. **البيئة الافتراضية:** يُنصح دائماً بتفعيل البيئة الافتراضية قبل التشغيل
2. **مفتاح API:** بدون `GEMINI_API_KEY` لن يعمل التطبيق
3. **الاتصال بالإنترنت:** التطبيق يحتاج اتصال بالإنترنت للاتصال بـ Gemini API
4. **إيقاف التطبيق:** اضغط `Ctrl + C` في Terminal لإيقاف التطبيق

---

## 🎯 مثال كامل للتشغيل

```powershell
# 1. الانتقال إلى مجلد المشروع
cd C:\Users\DELL\Desktop\Ai\storytelling_ai

# 2. تفعيل البيئة الافتراضية
.\venv\Scripts\Activate.ps1

# 3. التأكد من المكتبات
pip install -r requirements.txt

# 4. تشغيل التطبيق
streamlit run frontend/app.py
```

---

**بعد التشغيل، افتح المتصفح على `http://localhost:8501` واستمتع! 🎉**

