# 🔧 دليل حل المشاكل

## المشاكل الشائعة وحلولها

### 1. خطأ "GEMINI_API_KEY غير موجود"

**السبب:** ملف `.env` غير موجود أو المفتاح غير مضاف بشكل صحيح.

**الحل:**
1. تأكد من وجود ملف `.env` في مجلد `storytelling_ai`
2. افتح الملف وتأكد من وجود السطر:
   ```
   GEMINI_API_KEY=your_actual_key_here
   ```
3. لا تضع مسافات حول علامة `=`
4. تأكد من عدم وجود علامات اقتباس حول المفتاح

### 2. خطأ في توليد القصة

**الأسباب المحتملة:**
- مفتاح API غير صحيح
- انتهت حصة الاستخدام اليومية
- مشكلة في الاتصال بالإنترنت

**الحل:**
1. تحقق من صحة مفتاح API من [Google AI Studio](https://makersuite.google.com/app/apikey)
2. تأكد من اتصالك بالإنترنت
3. انتظر قليلاً إذا تجاوزت الحد اليومي

### 3. الصور لا تتولد بشكل صحيح

**السبب:** Hugging Face API قد يكون غير متاح أو بطيء.

**الحل:**
- التطبيق يستخدم صورة افتراضية جميلة إذا فشل توليد الصورة
- للحصول على صور أفضل، أضف `HUGGINGFACE_API_KEY` في ملف `.env`
- احصل على المفتاح من [Hugging Face](https://huggingface.co/settings/tokens)

### 4. النص العربي لا يظهر بشكل صحيح في الصورة

**السبب:** مشكلة في الخطوط العربية.

**الحل:**
- التطبيق يحاول استخدام خطوط Windows الافتراضية
- إذا لم تظهر بشكل صحيح، قد تحتاج لتثبيت خطوط عربية إضافية
- الخطوط المدعومة: Arial, Tahoma, Calibri

### 5. "ModuleNotFoundError"

**السبب:** المكتبات غير مثبتة.

**الحل:**
```bash
pip install -r requirements.txt
```

### 6. Streamlit لا يعمل

**الحل:**
```bash
pip install streamlit
streamlit run frontend/app.py
```

### 7. بطء في توليد القصة

**السبب:** طبيعي - قد يستغرق 10-30 ثانية.

**الحل:**
- انتظر قليلاً، العملية قد تستغرق وقتاً
- تأكد من سرعة اتصالك بالإنترنت

## الحصول على مفاتيح API

### Gemini API Key
1. اذهب إلى [Google AI Studio](https://makersuite.google.com/app/apikey)
2. سجل الدخول بحساب Google
3. اضغط "Create API Key"
4. انسخ المفتاح وأضفه في `.env`

### Hugging Face API Key (اختياري)
1. اذهب إلى [Hugging Face](https://huggingface.co/settings/tokens)
2. سجل الدخول أو أنشئ حساب
3. اضغط "New token"
4. اختر "Read" كصلاحية
5. انسخ المفتاح وأضفه في `.env`

## دعم إضافي

إذا استمرت المشاكل:
1. تحقق من إصدار Python (يجب أن يكون 3.8+)
2. تأكد من تثبيت جميع المكتبات
3. تحقق من ملف `.env` مرة أخرى
4. جرب إعادة تشغيل التطبيق

---

**نصيحة:** اقرأ ملف `QUICK_START.md` للبدء السريع!

