# ⚡ حل سريع لمشكلة GEMINI_API_KEY

## المشكلة
```
❌ Initialization error: GEMINI_API_KEY not found
```

## الحل السريع (3 خطوات):

### 1️⃣ اذهب إلى Secrets
- في صفحة التطبيق على Streamlit Cloud
- اضغط **⋮** (ثلاث نقاط) → **Settings** → **Secrets**

### 2️⃣ أضف المفتاح بهذا الشكل بالضبط:

```toml
GEMINI_API_KEY = "AIzaSyAV8M86_wj9EUfMs407tvYmKIAwiAF7sKs"
CUSTOM_IMAGE_API_KEY = "efba5c5ee632cb0e5b13ad97948a15b4f805bd948f855e7553a8693faa798b2e59fb76df19a360c015133a36926a9d7c"
USE_CLIPDROP = "true"
```

**⚠️ مهم جداً:**
- استخدم `=` مع **مسافات** قبل وبعد
- ضع القيم بين **علامات اقتباس** `"..."`
- لا تنس علامات الاقتباس!

### 3️⃣ احفظ وأعد التشغيل
- اضغط **"Save"**
- انتظر 30-60 ثانية
- اضغط **"Reboot app"** (في Settings)
- أعد تحميل الصفحة

---

## ✅ التحقق من أن المفتاح موجود:

بعد الحفظ، يجب أن ترى في Secrets:
```
GEMINI_API_KEY = "AIzaSy..."
```

إذا لم تراه، أعد إضافته.

---

## 🔍 إذا لم يعمل بعد:

1. **تحقق من الصيغة:**
   - يجب أن تكون TOML format
   - `KEY = "value"` وليس `KEY=value`

2. **تحقق من المفتاح:**
   - تأكد أن المفتاح صحيح
   - جرب نسخه من جديد

3. **أعد التشغيل:**
   - Settings → "Reboot app"
   - انتظر حتى ينتهي

4. **تحقق من Logs:**
   - Settings → Logs
   - ابحث عن أخطاء

---

**بعد هذه الخطوات، يجب أن يعمل التطبيق! 🎉**

