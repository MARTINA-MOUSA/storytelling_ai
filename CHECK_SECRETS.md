# 🔍 كيفية التحقق من أن Secrets تعمل

## المشكلة
أضفت المفاتيح في Streamlit Secrets لكن لا يزال يظهر الخطأ.

## الحلول:

### 1️⃣ تحقق من الصيغة في Secrets

اذهب إلى **Settings → Secrets** وتأكد أن الصيغة **بالضبط** كالتالي:

```toml
GEMINI_API_KEY = "AIzaSyAV8M86_wj9EUfMs407tvYmKIAwiAF7sKs"
CUSTOM_IMAGE_API_KEY = "efba5c5ee632cb0e5b13ad97948a15b4f805bd948f855e7553a8693faa798b2e59fb76df19a360c015133a36926a9d7c"
USE_CLIPDROP = "true"
```

**⚠️ أخطاء شائعة:**

❌ **خطأ:**
```toml
GEMINI_API_KEY="key"  # بدون مسافات
GEMINI_API_KEY=key    # بدون علامات اقتباس
GEMINI_API_KEY = key  # بدون علامات اقتباس
```

✅ **صحيح:**
```toml
GEMINI_API_KEY = "key"  # مع مسافات وعلامات اقتباس
```

### 2️⃣ أعد تشغيل التطبيق

بعد إضافة/تعديل Secrets:

1. اضغط **"Save"** في Secrets
2. انتظر 30-60 ثانية
3. اذهب إلى **Settings → "Reboot app"**
4. انتظر حتى ينتهي إعادة التشغيل
5. أعد تحميل الصفحة (F5 أو Ctrl+R)

### 3️⃣ استخدم Debug Info

في صفحة الخطأ، اضغط على **"🔍 Debug Info"** لترى:
- هل المفتاح موجود في environment variables؟
- هل المفتاح موجود في Streamlit secrets؟
- هل ملف config.py موجود؟

### 4️⃣ تحقق من Logs

1. اذهب إلى **Settings → Logs**
2. ابحث عن أي أخطاء متعلقة بـ API keys
3. تحقق من أن المفاتيح موجودة

### 5️⃣ جرب إعادة إضافة المفاتيح

1. احذف المفاتيح من Secrets
2. اضغط "Save"
3. أعد إضافتها بالصيغة الصحيحة
4. اضغط "Save"
5. أعد تشغيل التطبيق

---

## ✅ قائمة التحقق:

- [ ] الصيغة صحيحة: `KEY = "value"` (مع مسافات وعلامات اقتباس)
- [ ] اضغطت "Save" بعد إضافة المفاتيح
- [ ] أعدت تشغيل التطبيق (Reboot app)
- [ ] انتظرت 30-60 ثانية بعد الحفظ
- [ ] أعدت تحميل الصفحة

---

## 🆘 إذا لم يعمل بعد:

1. **تحقق من Debug Info** في صفحة الخطأ
2. **تحقق من Logs** في Settings
3. **جرب حذف وإعادة إضافة** المفاتيح
4. **تأكد من المفتاح** - جرب نسخه من جديد

---

**بعد هذه الخطوات، يجب أن يعمل! 🎉**

