# 🚀 دليل البدء السريع

## خطوات سريعة للبدء

### 1. تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### 2. إعداد مفاتيح API

1. انسخ `env_template.txt` إلى `.env`:
   ```bash
   copy env_template.txt .env
   ```

2. احصل على مفتاح Gemini API من [Google AI Studio](https://makersuite.google.com/app/apikey)

3. أضف المفتاح في ملف `.env`:
   ```
   GEMINI_API_KEY=your_key_here
   ```

### 3. تشغيل التطبيق

**Windows:**
```bash
run.bat
```

**أو يدوياً:**
```bash
streamlit run frontend/app.py
```

### 4. استخدام التطبيق

1. افتح المتصفح على `http://localhost:8501`
2. أدخل اسمك واختر نوع القصة
3. أضف الشخصيات والأحداث
4. اضغط "توليد القصة"
5. انتظر قليلاً وستحصل على قصتك مع صورة!

## ملاحظات مهمة

- **GEMINI_API_KEY**: مطلوب (للحصول على القصص)
- **HUGGINGFACE_API_KEY**: اختياري (لتحسين جودة الصور)
- بدون Hugging Face API، سيتم استخدام صورة افتراضية جميلة

## حل المشاكل

### خطأ "GEMINI_API_KEY غير موجود"
- تأكد من وجود ملف `.env` في مجلد المشروع
- تأكد من كتابة المفتاح بشكل صحيح

### التطبيق لا يعمل
- تأكد من تثبيت جميع المكتبات: `pip install -r requirements.txt`
- تأكد من وجود Python 3.8 أو أحدث

---

**استمتع بإنشاء قصصك! 📚✨**

